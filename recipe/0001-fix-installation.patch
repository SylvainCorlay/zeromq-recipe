From c9e665451b9bc33d284ceb2e70efe2338e7fb785 Mon Sep 17 00:00:00 2001
From: gouarin <loic.gouarin@gmail.com>
Date: Wed, 6 Dec 2017 08:54:11 +0100
Subject: [PATCH] fix installation

---
 CMakeLists.txt | 11 ++++++-----
 1 file changed, 6 insertions(+), 5 deletions(-)

diff --git a/CMakeLists.txt b/CMakeLists.txt
index a14e59e5..793f5979 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -345,12 +345,13 @@ elseif (CMAKE_SYSTEM_NAME MATCHES "Darwin")
   add_definitions (-D_DARWIN_C_SOURCE)
 endif ()
 
-set (CMAKE_PYTHON_VERSION 2.7 2.6 2.5 2.4)
-find_package (PythonInterp)
-find_package (AsciiDoc)
+# set (CMAKE_PYTHON_VERSION 2.7 2.6 2.5 2.4)
+# find_package (PythonInterp)
+# find_package (AsciiDoc)
 
-cmake_dependent_option (WITH_DOC "Build Reference Guide documentation (requires DocBook)" ON
-                       "PYTHONINTERP_FOUND;ASCIIDOC_FOUND" OFF)
+# cmake_dependent_option (WITH_DOC "Build Reference Guide documentation (requires DocBook)" ON
+#                        "PYTHONINTERP_FOUND;ASCIIDOC_FOUND" OFF)
+option (WITH_DOC "Build Reference Guide documentation (requires DocBook)" OFF)
 
 if (MSVC)
   if (WITH_OPENPGM)
diff --git a/ZeroMQConfig.cmake.in b/ZeroMQConfig.cmake.in
new file mode 100644
index 0000000..bfdb981
--- /dev/null
+++ b/ZeroMQConfig.cmake.in
+@@ -0,0 +1,25 @@
+# ZeroMQ cmake module
+#
+# The following import targets are created
+#
+# ::
+#
+#   libzmq-static
+#   libzmq
+#
+# This module sets the following variables in your project::
+#
+#   ZeroMQ_FOUND - true if ZeroMQ found on the system
+#   ZeroMQ_INCLUDE_DIR - the directory containing ZeroMQ headers
+#   ZeroMQ_LIBRARY - 
+#   ZeroMQ_STATIC_LIBRARY
+
+@PACKAGE_INIT@
+
+if(NOT TARGET libzmq AND NOT TARGET libzmq-static)
+  include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@Targets.cmake")
+
+  get_target_property(@PROJECT_NAME@_INCLUDE_DIR libzmq INTERFACE_INCLUDE_DIRECTORIES)
+  get_target_property(@PROJECT_NAME@_LIBRARY libzmq LOCATION)
+  get_target_property(@PROJECT_NAME@_STATIC_LIBRARY libzmq-static LOCATION)
+endif()
-- 
2.14.1

